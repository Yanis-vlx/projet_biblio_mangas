{% extends 'base.html.twig' %}

{% block title %}Catalogue{% endblock %}

{% block body %}

<<<<<<< HEAD
<main class="container mt-4 pt-5 text-center text-black">
    <h2>Catalogue</h2>
        {{ form_start(form, { 'attr': {'class': 'd-flex'} }) }}
            {{ form_widget(form.q, { 'attr': {'class': 'form-control me-2 border border-dark', 'placeholder': 'Rechercher...'} }) }}
                <button type="submit" class="btn btn-outline-dark">🔍</button>
        {{ form_end(form) }}
        <form method="get" class="d-flex gap-2 mb-4 justify-content-center">
            <select name="genre" class="form-select w-auto">
=======
<main class="container mt-5 pt-5 text-center text-black">
    <div class="d-flex justify-content-center align-items-start flex-wrap gap-3">
        <div>
            {{ form_start(form, { 'attr': {'class': 'd-flex'} }) }}
                {{ form_widget(form.q, { 'attr': {'class': 'form-control me-2 border border-dark', 'placeholder': 'Rechercher...'} }) }}
                    <button type="submit" class="btn btn-outline-dark">🔍</button>
            {{ form_end(form) }}
        </div>
        <form method="get" class="d-flex gap-2 justify-content-center">
            <select name="genre" class="form-select w-auto border border-dark">
>>>>>>> 7725b0c3ef619896f07c7ee44ee1ac2e401a68f0
                <option value="">Tous les genres</option>
                <option value="Shonen" {% if app.request.get('genre') == 'Shonen' %}selected{% endif %}>Shonen</option>
                <option value="Shojo" {% if app.request.get('genre') == 'Shojo' %}selected{% endif %}>Shojo</option>
                <option value="Seinen" {% if app.request.get('genre') == 'Seinen' %}selected{% endif %}>Seinen</option>
            </select>

<<<<<<< HEAD
            <select name="order" class="form-select w-auto">
=======
            <select name="order" class="form-select w-auto border border-dark">
>>>>>>> 7725b0c3ef619896f07c7ee44ee1ac2e401a68f0
                <option value="ASC" {% if app.request.get('order') == 'ASC' %}selected{% endif %}>A → Z</option>
                <option value="DESC" {% if app.request.get('order') == 'DESC' %}selected{% endif %}>Z → A</option>
            </select>

            <button type="submit" class="btn btn-outline-dark mt-auto">Filtrer</button>
        </form>
<<<<<<< HEAD
        <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">
        {% for manga in mangas %}
        <div class="col m-5">
            <div class="card w-100 mb-2 rounded">
                <img src={{ manga.cover }} alt={{ manga.title }} />
            </div>
                <a class="btn btn-outline-dark mt-auto text-black w-50" href="{{ path('app_manga_show', {id: manga.id}) }}"> {{ manga.title }} </a>
=======
    </div>

        <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">
        {% for manga in mangas %}
        <div class="col m-5">
            <a class="btn mt-auto text-black w-70" href="{{ path('app_manga_show', {id: manga.id}) }}">
                <img class="rounded" src={{ manga.cover }} alt={{ manga.title }} />
            </a>
>>>>>>> 7725b0c3ef619896f07c7ee44ee1ac2e401a68f0
        </div>

        {% else %}
            <h3>Aucun mangas à afficher</h3>
        {% endfor %}
    </div>
</main>
{% endblock %}
